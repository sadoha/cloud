---

# Dependencies for the tool installation and configuration.
dependencies:
  - { role: common }
  - { role: docker }
  - { role: kubernetes/common , pkgs: [ "kubectl", "kubelet", "kubernetes-cni", "kubeadm" ] }

