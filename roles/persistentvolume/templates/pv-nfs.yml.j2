apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: {{ kube_master_ip }}
    # Exported path of your NFS server
    path: "/var/nfsshare"
